type User={id:string;email:string;name:string;hospitalId:string;hospitalName:string;role:string};
type Inventory={id:string;medicine:string;batch:string;expiry:string;quantity:number;dailyRunRate:number;unitCost:number;status:string};
const $=<T extends HTMLElement>(s:string)=>document.querySelector<T>(s)!;
let token=localStorage.getItem('mr_v2_token')||'';let user:User|null=null;let current='dashboard';
const api=async<T>(path:string,init:RequestInit={})=>{const h=new Headers(init.headers);h.set('Content-Type','application/json');if(token)h.set('Authorization',`Bearer ${token}`);const r=await fetch(path,{...init,headers:h});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Request failed');return d as T};
const formObject=(f:FormData)=>{const o:any={};f.forEach((v,k)=>o[k]=String(v));for(const k of ['quantity','dailyRunRate','unitCost'])if(k in o)o[k]=Number(o[k]);return o};
const toast=(m:string)=>{const e=$('#toast');e.textContent=m;e.classList.add('show');setTimeout(()=>e.classList.remove('show'),2600)};
function shell(){document.body.innerHTML=`<div class="app"><aside><div class="brand"><span>✚</span><b>MedRebalance <i>V2</i></b></div><div class="workspace">${user?.hospitalName||'Workspace'}<small>${user?.role||''}</small></div><nav>${[['dashboard','◈','Dashboard'],['inventory','▦','Inventory'],['stockouts','⚠','Stockouts'],['transfers','⇄','Transfers'],['analytics','◔','Analytics'],['notifications','◉','Notifications'],['billing','◫','Billing']].map(x=>`<button class="nav ${current===x[0]?'active':''}" data-page="${x[0]}"><span>${x[1]}</span>${x[2]}</button>`).join('')}</nav><button id="logout" class="logout">↪ Sign out</button></aside><main><header><div><span class="eyebrow">MEDICAL SUPPLY NETWORK</span><h1 id="title">Dashboard</h1></div><div class="top"><span class="status">● System online</span><button id="bell">◉</button><div class="avatar">${(user?.name||'U')[0]}</div></div></header><section id="content"></section></main></div><div id="toast" class="toast"></div>`;document.querySelectorAll<HTMLButtonElement>('.nav').forEach(b=>b.onclick=()=>{current=b.dataset.page!;render()});$('#logout').onclick=async()=>{try{await api('/api/v2/auth/logout',{method:'POST'})}catch{}localStorage.removeItem('mr_v2_token');token='';login()};render()}
function card(label:string,value:string,meta:string,icon:string){return `<div class="metric"><div class="metric-icon">${icon}</div><span>${label}</span><strong>${value}</strong><small>${meta}</small></div>`}
async function render(){if(!user)return;const titles:any={dashboard:'Dashboard',inventory:'Inventory',stockouts:'Stockout Requests',transfers:'Transfers',analytics:'Analytics',notifications:'Notifications',billing:'Billing'};$('#title').textContent=titles[current]||'Dashboard';try{if(current==='dashboard')return dashboard();if(current==='inventory')return inventory();if(current==='stockouts')return stockouts();if(current==='transfers')return transfers();if(current==='analytics')return analytics();if(current==='notifications')return notifications();if(current==='billing')return billing()}catch(e){toast((e as Error).message)}}
async function dashboard(){const d=await api<any>('/api/v2/dashboard');$('#content').innerHTML=`<div class="hero"><div><span class="pill">V2 CONTROL CENTER</span><h2>Good morning, ${user!.name.split(' ')[0]}.</h2><p>Keep medicine available, reduce expiry waste and coordinate hospitals from one workspace.</p></div><div class="hero-art">✚<b>LIVE</b></div></div><div class="metrics">${user!.role==='network_admin'?card('Total hospitals',d.totalHospitals,'Across the network','⌂'):card('Medicine batches',d.totalMedicines,'In your hospital','▦')}${card('Low stock',d.lowStock,'Needs attention','⚠')}${card('Expiring soon',d.expiring,'Within 30 days','◷')}${card('Pending transfers',d.pendingTransfers??0,'Awaiting action','⇄')}</div><div class="grid2"><div class="panel"><div class="panel-head"><h3>Inventory health</h3><span class="tag good">LIVE</span></div><div class="bars"><i style="height:78%"></i><i style="height:55%"></i><i style="height:68%"></i><i style="height:45%"></i><i style="height:82%"></i><i style="height:61%"></i><i style="height:88%"></i></div><div class="legend"><span>Jan</span><span>Feb</span><span>Mar</span><span>Apr</span><span>May</span><span>Jun</span><span>Jul</span></div></div><div class="panel quick"><div class="panel-head"><h3>Quick actions</h3></div><button data-page="inventory">＋ Add inventory</button><button data-page="stockouts">⚠ Request stock</button><button data-page="transfers">⇄ Create transfer</button></div></div>`;document.querySelectorAll<HTMLButtonElement>('[data-page]').forEach(b=>b.onclick=()=>{current=b.dataset.page!;render()})}
async function inventory(){const items=await api<Inventory[]>('/api/v2/inventory');$('#content').innerHTML=`<div class="page-actions"><div><p class="muted">Manage medicine batches, expiry and stock levels.</p></div><button class="primary" id="addInv">＋ Add batch</button></div><div class="panel table-wrap"><input class="search" id="search" placeholder="Search medicine or batch…"><table><thead><tr><th>Medicine</th><th>Batch</th><th>Expiry</th><th>Stock</th><th>Run rate</th><th>Status</th></tr></thead><tbody>${items.map(x=>`<tr><td><b>${x.medicine}</b></td><td>${x.batch}</td><td>${x.expiry}</td><td>${x.quantity}</td><td>${x.dailyRunRate}/day</td><td><span class="tag ${x.status==='In stock'?'good':x.status==='Critical'?'bad':'warn'}">${x.status}</span></td></tr>`).join('')||`<tr><td colspan="6" class="empty">No inventory yet. Add your first batch.</td></tr>`}</tbody></table></div>`;$('#addInv').onclick=()=>modal('Add inventory batch',`<form id="invForm"><label>Medicine<input name="medicine" required placeholder="Paracetamol 500mg"></label><label>Batch<input name="batch" required placeholder="B-2026-001"></label><label>Expiry<input name="expiry" type="date" required></label><div class="row"><label>Quantity<input name="quantity" type="number" min="1" required></label><label>Daily run rate<input name="dailyRunRate" type="number" min="1" value="5" required></label></div><label>Unit cost (₹)<input name="unitCost" type="number" min="0.01" step="0.01" required></label><button class="primary" type="submit">Save batch</button></form>`);$('#search').oninput=(e)=>{const q=(e.target as HTMLInputElement).value.toLowerCase();document.querySelectorAll('tbody tr').forEach(r=>r.classList.toggle('hidden',!r.textContent!.toLowerCase().includes(q)))}}
async function stockouts(){const items=await api<any[]>('/api/v2/stockouts');$('#content').innerHTML=`<div class="page-actions"><p class="muted">Raise and track shortages before they become service interruptions.</p><button class="primary" id="newReq">＋ New request</button></div><div class="panel table-wrap"><table><thead><tr><th>Medicine</th><th>Quantity</th><th>Priority</th><th>Status</th><th>Created</th></tr></thead><tbody>${items.map(x=>`<tr><td><b>${x.medicine}</b></td><td>${x.quantity}</td><td><span class="tag ${x.priority==='emergency'?'bad':'warn'}">${x.priority}</span></td><td>${x.status}</td><td>${x.created}</td></tr>`).join('')||`<tr><td colspan="5" class="empty">No stockout requests.</td></tr>`}</tbody></table></div>`;$('#newReq').onclick=()=>modal('New stockout request',`<form id="stockForm"><label>Medicine<input name="medicine" required placeholder="Insulin (Human)"></label><label>Quantity needed<input name="quantity" type="number" min="1" required></label><label>Priority<select name="priority"><option value="routine">Routine</option><option value="urgent">Urgent</option><option value="emergency">Emergency</option></select></label><button class="primary" type="submit">Submit request</button></form>`)}
async function transfers(){const items=await api<any[]>('/api/v2/transfers');const hs=await api<any[]>('/api/v2/hospitals');$('#content').innerHTML=`<div class="page-actions"><p class="muted">Coordinate medicine movement between connected hospitals.</p><button class="primary" id="newTransfer">＋ New transfer</button></div><div class="panel table-wrap"><table><thead><tr><th>Medicine</th><th>From</th><th>To</th><th>Qty</th><th>Status</th><th>Date</th></tr></thead><tbody>${items.map(x=>`<tr><td><b>${x.medicine}</b></td><td>${x.from}</td><td>${x.to}</td><td>${x.quantity}</td><td><span class="tag ${x.status==='completed'?'good':'warn'}">${x.status}</span></td><td>${x.date}</td></tr>`).join('')||`<tr><td colspan="6" class="empty">No transfers yet.</td></tr>`}</tbody></table></div>`;$('#newTransfer').onclick=()=>modal('Create transfer',`<form id="transferForm"><label>Medicine<input name="medicine" required placeholder="Ceftriaxone 1g"></label><label>Destination<select name="toHospital">${hs.filter(x=>x.id!==user!.hospitalId).map(x=>`<option value="${x.id}">${x.name}</option>`).join('')}</select></label><label>Quantity<input name="quantity" type="number" min="1" required></label><button class="primary" type="submit">Create transfer</button></form>`)}
async function analytics(){const d=await api<any>('/api/v2/analytics');$('#content').innerHTML=`<div class="metrics">${card('Inventory batches',d.inventoryBatches,'Current','▦')}${card('Low stock',d.lowStock,'Needs action','⚠')}${card('Expired',d.expired,'Past expiry date','◷')}${card('Fulfillment rate',d.fulfillmentRate+'%','Network target','✓')}</div><div class="grid2"><div class="panel"><h3>Medicine usage trend</h3><div class="bars tall"><i style="height:42%"></i><i style="height:55%"></i><i style="height:49%"></i><i style="height:68%"></i><i style="height:72%"></i><i style="height:86%"></i><i style="height:94%"></i></div></div><div class="panel donut"><div class="donut-ring"></div><div><h3>Inventory distribution</h3><p class="muted">Monitor stock health across active batches.</p></div></div></div>`}
async function notifications(){const items=await api<any[]>('/api/v2/notifications');$('#content').innerHTML=`<div class="page-actions"><p class="muted">Operational alerts, transfer updates and expiry reminders.</p><button class="secondary" id="readAll">Mark all read</button></div><div class="panel feed">${items.map(x=>`<article class="notice ${x.readAt?'read':''}"><div class="notice-icon">${x.type==='stockout'?'⚠':'◉'}</div><div><b>${x.title}</b><p>${x.message}</p><small>${x.created}</small></div></article>`).join('')||'<div class="empty">You are all caught up.</div>'}</div>`;$('#readAll').onclick=async()=>{await api('/api/v2/notifications/read',{method:'POST'});toast('Notifications marked as read');render()}}
async function billing(){const d=await api<any>('/api/v2/billing');$('#content').innerHTML=`<div class="panel billing"><span class="pill">CURRENT PLAN</span><h2>${d.plan}</h2><div class="price">₹${Number(d.monthlyAmount).toLocaleString('en-IN')}<small>/month</small></div><p class="muted">Centralized inventory, rebalancing and hospital collaboration.</p><div class="billing-grid"><div><b>✓ Multi-hospital workspace</b><b>✓ Inventory analytics</b><b>✓ Stockout coordination</b><b>✓ Audit-ready operations</b></div><div class="invoice"><span>Status</span><strong>${d.status}</strong><span>Currency</span><strong>${d.currency}</strong></div></div></div>`}
function modal(title:string,body:string){const wrap=document.createElement('div');wrap.className='modal-backdrop';wrap.innerHTML=`<div class="modal"><div class="modal-head"><h2>${title}</h2><button id="close">×</button></div>${body}</div>`;document.body.append(wrap);wrap.querySelector('#close')!.addEventListener('click',()=>wrap.remove());const form=wrap.querySelector('form');if(form){form.addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(form);try{if(form.id==='invForm')await api('/api/v2/inventory',{method:'POST',body:JSON.stringify(formObject(f))});if(form.id==='stockForm')await api('/api/v2/stockouts',{method:'POST',body:JSON.stringify(formObject(f))});if(form.id==='transferForm')await api('/api/v2/transfers',{method:'POST',body:JSON.stringify(formObject(f))});wrap.remove();toast('Saved successfully');render()}catch(err){toast((err as Error).message)}})}}
function login(){document.body.innerHTML=`<div class="auth"><div class="auth-brand">✚ <b>MedRebalance <i>V2</i></b></div><div class="auth-card"><div class="auth-copy"><span class="pill">MEDICAL SUPPLY NETWORK</span><h1>Smarter inventory.<br><em>Healthier tomorrow.</em></h1><p>Prevent medicine shortages, reduce expiry waste and coordinate hospital supply in one secure workspace.</p><div class="stats"><b>500+<small>Hospitals</small></b><b>1M+<small>Units balanced</small></b><b>40%<small>Less expiry waste</small></b></div></div><form id="loginForm"><h2>Welcome back</h2><p class="muted">Sign in to continue to your workspace.</p><label>Email<input name="email" type="email" required></label><label>Password<input name="password" type="password" placeholder="Your password" required></label><button class="primary" type="submit">Sign in →</button><div class="divider">New hospital?</div><button type="button" class="secondary" id="register">Create workspace</button></form></div></div>`;$('#loginForm').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.currentTarget as HTMLFormElement);try{const d=await api<any>('/api/v2/auth/login',{method:'POST',body:JSON.stringify(formObject(f))});token=d.token;user=d.user;localStorage.setItem('mr_v2_token',token);shell()}catch(err){toast((err as Error).message)}});$('#register').onclick=()=>register()}
function register(){document.body.innerHTML=`<div class="auth"><div class="auth-brand">✚ <b>MedRebalance <i>V2</i></b></div><form class="register-card" id="registerForm"><span class="pill">NEW WORKSPACE</span><h1>Create your hospital workspace</h1><p class="muted">Start managing inventory with MedRebalance V2.</p><label>Hospital name<input name="hospitalName" required placeholder="City Care Hospital"></label><label>Your name<input name="userName" required placeholder="Dr. A. Sharma"></label><label>Email<input name="email" type="email" required></label><label>Password<input name="password" type="password" minlength="8" required></label><button class="primary">Create workspace →</button><button type="button" class="secondary" id="back">Back to sign in</button></form></div>`;$('#registerForm').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.currentTarget as HTMLFormElement);try{const d=await api<any>('/api/v2/auth/register',{method:'POST',body:JSON.stringify(formObject(f))});token=d.token;user=d.user;localStorage.setItem('mr_v2_token',token);shell()}catch(err){toast((err as Error).message)}});$('#back').onclick=login}
async function boot(){if(token){try{const d=await api<any>('/api/v2/me');user=d.user;shell();return}catch{localStorage.removeItem('mr_v2_token');token=''}}login()}
boot();
